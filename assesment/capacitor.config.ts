import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'assesment',
  webDir: 'dist/assesment',
  server: {
    androidScheme: 'https'
  }
};

export default config;
